to run the feature extraction in the command line do as was instructed 
this will not run in numpy is not installed

the features will be appended to each row as a numpy array in a column with the header 'features'


python feature_extraction.py input.csv output.csv


the maxe_decision_tree.py code will not run unless the following libraries are installed

pandas
sklearn
numpy